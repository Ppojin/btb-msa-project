import { container, title } from "assets/jss/jssMain2.js";

const HomeBasics = {
    sections: {
        padding: "70px 0"
    },
    container,
    title: {
        ...title,
        marginTop: "30px",
        minHeight: "32px",
        textDecoration: "none"
    },
};

export default HomeBasics;
